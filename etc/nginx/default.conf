# Generic Nginx Configuration Template with Environment Variables
# Add your project-specific server blocks here

# ==========================================
# EXAMPLES BELOW - Copy and modify for new projects
# ==========================================

# Example Laravel/Symfony PHP Project with variable hostname
# server {
#     listen 80;
#     server_name myapp.local;
#
#     index index.php index.html;
#     error_log  /var/log/nginx/error.log;
#     access_log /var/log/nginx/access.log;
#     root /var/www/myapp/public;
#
#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }
#
#     location ~ \.php$ {
#         try_files $uri =404;
#         fastcgi_split_path_info ^(.+\.php)(/.+)$;
#         fastcgi_pass laravel-myapp:9000;  # Match your service name
#         fastcgi_index index.php;
#         include fastcgi_params;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#         fastcgi_param PATH_INFO $fastcgi_path_info;
#     }
# }

# SSL Example (optional)
# Use project-specific certificates generated by add-project script
# server {
#     listen 443 ssl;
#     server_name myapp.local;
#
#     ssl_certificate /etc/ssl/myapp/myapp.crt;
#     ssl_certificate_key /etc/ssl/myapp/myapp.key;
#     ssl_protocols TLSv1.2 TLSv1.3;
#
#     index index.php index.html;
#     error_log  /var/log/nginx/error.log;
#     access_log /var/log/nginx/access.log;
#     root /var/www/myapp/public;
#
#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }
#
#     location ~ \.php$ {
#         try_files $uri =404;
#         fastcgi_split_path_info ^(.+\.php)(/.+)$;
#         fastcgi_pass laravel-myapp:9000;
#         fastcgi_index index.php;
#         include fastcgi_params;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#         fastcgi_param PATH_INFO $fastcgi_path_info;
#     }
# }

# Example React Project with Hot Reload
# server {
#     listen 80;
#     server_name myreactapp.local;
#
#     location / {
#         proxy_pass http://react-myapp:5173;  # Match your service name
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_cache_bypass $http_upgrade;
#     }
# }

# Add this to etc/nginx/default.conf for virtual host access:
server {
    listen 80;
    server_name myapp.local;

    index index.php index.html;
    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;
    root /var/www/php/myapp/public;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass laravel-myapp:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME /var/www/myapp/public$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }
}

# HTTPS (optional):
server {
    listen 443 ssl;
    server_name myapp.local;

    ssl_certificate /etc/ssl/myapp/myapp.crt;
    ssl_certificate_key /etc/ssl/myapp/myapp.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    index index.php index.html;
    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;
    root /var/www/php/myapp/public;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass laravel-myapp:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME /var/www/myapp/public$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }
}

# Add this to etc/nginx/default.conf for virtual host access:
server {
    listen 80;
    server_name myapi.local;

    index index.php index.html;
    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;
    root /var/www/php/myapi/public;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass symfony-myapi:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME /var/www/myapi/public$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }
}

# HTTPS (optional):
server {
    listen 443 ssl;
    server_name myapi.local;

    ssl_certificate /etc/ssl/myapi/myapi.crt;
    ssl_certificate_key /etc/ssl/myapi/myapi.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    index index.php index.html;
    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;
    root /var/www/php/myapi/public;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass symfony-myapi:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME /var/www/myapi/public$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }
}

# # React Project: frontend
# server {
#     listen 80;
#     server_name frontend.local;

#     location / {
#         proxy_pass http://react-frontend:5173;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_cache_bypass $http_upgrade;
#     }
# }

# # HTTPS for frontend
# server {
#     listen 443 ssl;
#     server_name frontend.local;

#     ssl_certificate /etc/ssl/frontend/frontend.crt;
#     ssl_certificate_key /etc/ssl/frontend/frontend.key;
#     ssl_protocols TLSv1.2 TLSv1.3;

#     location / {
#         proxy_pass http://react-frontend:5173;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_cache_bypass $http_upgrade;
#     }
# }

# Add this to etc/nginx/default.conf for virtual host access:
server {
    listen 80;
    server_name frontend.local;

    location / {
        proxy_pass http://react-frontend:5173;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_cache_bypass $http_upgrade;
    }
}

# HTTPS (optional):
server {
    listen 443 ssl;
    server_name frontend.local;

    ssl_certificate /etc/ssl/frontend/frontend.crt;
    ssl_certificate_key /etc/ssl/frontend/frontend.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
        proxy_pass http://react-frontend:5173;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_cache_bypass $http_upgrade;
    }
}
